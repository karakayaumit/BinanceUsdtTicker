<Window x:Class="BinanceUsdtTicker.AddAlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Alarm Ekle"
        Width="420" Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource Surface}"
        Foreground="{DynamicResource OnSurface}">

    <Grid Margin="16" ShowGridLines="True">
        <!-- DOĞRU: Koleksiyon şeklinde tanımla -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sembol -->
        <TextBlock Text="Sembol:" Grid.Row="0" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center"/>
        <TextBox x:Name="SymbolBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" />

        <!-- Koşul -->
        <TextBlock Text="Koşul:" Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center"/>
        <ComboBox x:Name="TypeBox"
                  Grid.Row="1"
                  Grid.Column="1"
                  Margin="0,0,0,8"
                  SelectedIndex="0">
            <ComboBoxItem Content="Fiyat ≥" Tag="PriceAtOrAbove" />
            <ComboBoxItem Content="Fiyat ≤" Tag="PriceAtOrBelow" />
            <ComboBoxItem Content="Fiyat [min,max]" Tag="PriceBetween" />
            <ComboBoxItem Content="Anlık Değişim ≥" Tag="ChangeSinceStartAtOrAbove" />
            <ComboBoxItem Content="Anlık Değişim ≤" Tag="ChangeSinceStartAtOrBelow" />
        </ComboBox>

        <!-- Değer(ler) -->
        <TextBlock Text="Değer(ler):" Grid.Row="2" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox x:Name="ABox" Width="120" Margin="0,0,8,0" />
            <TextBox x:Name="BBox" Width="120" Visibility="Collapsed" />
        </StackPanel>

        <!-- One-shot & Cooldown -->
        <CheckBox x:Name="OneShotBox" Grid.Row="3" Grid.ColumnSpan="2" Content="Tek seferlik" IsChecked="True" Margin="0,0,0,8"/>
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Cooldown (sn):" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="CooldownBox" Width="80" Text="0"/>
        </StackPanel>

        <!-- Butonlar -->
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="İptal" Width="100" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Ekle"  Width="100" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
