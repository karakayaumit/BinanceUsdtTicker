<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== Palette (DARK) ===== -->
    <SolidColorBrush x:Key="Surface"        Color="#FF0B0E11"/>
    <SolidColorBrush x:Key="SurfaceAlt"     Color="#FF181A20"/>
    <SolidColorBrush x:Key="OnSurface"      Color="#FFEAECEF"/>
    <SolidColorBrush x:Key="SubtleText"     Color="#FF9B9B9B"/>
    <SolidColorBrush x:Key="Divider"        Color="#FF2E3239"/>

    <SolidColorBrush x:Key="HeaderBg"       Color="#FF0B0E11"/>
    <SolidColorBrush x:Key="HeaderFg"       Color="#FFEAECEF"/>
    <SolidColorBrush x:Key="HeaderBorder"   Color="#FF2E3239"/>

    <SolidColorBrush x:Key="RowBg"          Color="#FF181A20"/>
    <SolidColorBrush x:Key="RowAltBg"       Color="#FF0F1114"/>

    <SolidColorBrush x:Key="SelectionBg"    Color="#FF1D4FCB"/>
    <SolidColorBrush x:Key="SelectionFg"    Color="#FFFFFFFF"/>

    <!-- Toolbar tokens -->
    <SolidColorBrush x:Key="TbBg"           Color="#FF181A20"/>
    <SolidColorBrush x:Key="TbBorder"       Color="#FF2E3239"/>
    <SolidColorBrush x:Key="TbFg"           Color="#FFEAECEF"/>
    <SolidColorBrush x:Key="TbFgMuted"      Color="#FF9B9B9B"/>
    <SolidColorBrush x:Key="TbHover"        Color="#FF20252C"/>
    <SolidColorBrush x:Key="TbPressed"      Color="#FF2B3139"/>
    <SolidColorBrush x:Key="TbChecked"      Color="#FF0E9C6A"/>
    <SolidColorBrush x:Key="TbCheckedFg"    Color="#FF05090D"/>
    <SolidColorBrush x:Key="TbDisabledBg"   Color="#33181A20"/>
    <SolidColorBrush x:Key="TbDisabledFg"   Color="#66C5C7CB"/>

    <!-- Button tokens -->
    <SolidColorBrush x:Key="BtnBg"      Color="#FF181A20"/>
    <SolidColorBrush x:Key="BtnHover"   Color="#FF20252C"/>
    <SolidColorBrush x:Key="BtnPressed" Color="#FF2B3139"/>

    <!-- Hover color for list rows -->
    <SolidColorBrush x:Key="RowHoverBg"      Color="#FF20252C"/>

    <!-- Top Movers tokens (dark) -->
    <SolidColorBrush x:Key="Up1Bg"          Color="#FF0E9C6A"/>
    <SolidColorBrush x:Key="Up3Bg"          Color="#FF006400"/>
    <SolidColorBrush x:Key="Down1Bg"        Color="#FFF08080"/>
    <SolidColorBrush x:Key="Down3Bg"        Color="#FF8B0000"/>
    <SolidColorBrush x:Key="Up1Fg"          Color="#FFEAECEF"/>
    <SolidColorBrush x:Key="Up3Fg"          Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="Down1Fg"        Color="#FF000000"/>
    <SolidColorBrush x:Key="Down3Fg"        Color="#FFFFFFFF"/>

    <!-- System colors for controls -->
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}"       Color="#FF181A20"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}"   Color="#FFEAECEF"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"      Color="#FF181A20"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}"  Color="#FFEAECEF"/>

    <!-- ===== ScrollViewer with visible scrollbars ===== -->
    <Style TargetType="{x:Type ScrollViewer}">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                    Grid.Row="0" Grid.Column="0"
                                    Content="{TemplateBinding Content}"
                                    CanContentScroll="{TemplateBinding CanContentScroll}"/>

                        <ScrollBar x:Name="PART_VerticalScrollBar"
                       Grid.Row="0" Grid.Column="1"
                       Orientation="Vertical"
                       Background="{DynamicResource SurfaceAlt}"
                       Foreground="{DynamicResource SubtleText}"
                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                       Grid.Row="1" Grid.Column="0"
                       Orientation="Horizontal"
                       Background="{DynamicResource SurfaceAlt}"
                       Foreground="{DynamicResource SubtleText}"
                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>

                        <Border Grid.Row="1" Grid.Column="1"
                    Background="{DynamicResource SurfaceAlt}"
                    BorderBrush="{DynamicResource Divider}"
                    BorderThickness="1"
                    Visibility="Collapsed"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Base ===== -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource Surface}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
    </Style>
    <Style TargetType="DockPanel">
        <Setter Property="LastChildFill" Value="True"/>
    </Style>
    <Style TargetType="Border">
        <Setter Property="BorderBrush" Value="{DynamicResource Divider}"/>
    </Style>
    <Style TargetType="Grid">
        <Setter Property="Background" Value="{x:Null}"/>
    </Style>
    <Style TargetType="StackPanel">
        <Setter Property="Background" Value="{x:Null}"/>
    </Style>
    <Style TargetType="Expander">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Divider}"/>
    </Style>
    <Style TargetType="GroupBox">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Divider}"/>
    </Style>
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Divider}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style TargetType="ListView">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Divider}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Divider}"/>
    </Style>
    <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Divider}"/>
    </Style>
    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource SelectionBg}"/>
                <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
        <Setter Property="Background" Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Divider}"/>
    </Style>

    <!-- ===== DataGrid ===== -->
    <Style TargetType="{x:Type DataGrid}">
        <Setter Property="Background"               Value="{DynamicResource SurfaceAlt}"/>
        <Setter Property="Foreground"               Value="{DynamicResource OnSurface}"/>
        <Setter Property="RowBackground"            Value="{DynamicResource RowBg}"/>
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource RowAltBg}"/>
        <Setter Property="GridLinesVisibility"      Value="All"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Divider}"/>
        <Setter Property="VerticalGridLinesBrush"   Value="{DynamicResource Divider}"/>
        <Setter Property="BorderBrush"              Value="{DynamicResource Divider}"/>
        <Setter Property="BorderThickness"         Value="1"/>
    </Style>
    <Style TargetType="{x:Type DataGridRow}">
        <Setter Property="Background" Value="{DynamicResource RowBg}"/>
        <Setter Property="Foreground" Value="{DynamicResource OnSurface}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource SelectionBg}"/>
                <Setter Property="Foreground" Value="{DynamicResource SelectionFg}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background"  Value="{DynamicResource HeaderBg}"/>
        <Setter Property="Foreground"  Value="{DynamicResource HeaderFg}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource HeaderBorder}"/>
        <Setter Property="FontWeight"  Value="SemiBold"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
    </Style>
    <Style TargetType="{x:Type DataGridCell}">
        <Setter Property="Background" Value="{x:Null}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background"  Value="{DynamicResource SelectionBg}"/>
                <Setter Property="Foreground"  Value="{DynamicResource SelectionFg}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
