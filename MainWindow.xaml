<Window x:Class="BinanceUsdtTicker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Binance USDT Canlı Fiyatlar" Height="680" Width="1000" MinHeight="500" MinWidth="800">
    <DockPanel LastChildFill="True">
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Hazır" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="LastUpdateText" />
            </StatusBarItem>
        </StatusBar>

        <ToolBar DockPanel.Dock="Top">
            <TextBlock Text="Ara:" Margin="0,0,6,0"/>
            <TextBox x:Name="SearchBox" Width="200" Margin="0,0,12,0" TextChanged="SearchBox_TextChanged" />
            <Button Click="RefreshSymbols_Click" Content="Sembolleri Yenile" />
            <Separator/>
            <CheckBox x:Name="OnlyPositiveChange" Content="% Değişim &gt; 0" Margin="8,0,0,0" Checked="FilterChanged" Unchecked="FilterChanged"/>
        </ToolBar>

        <Grid>
            <DataGrid x:Name="Grid"
                      AutoGenerateColumns="False"
                      EnableRowVirtualization="True"
                      IsReadOnly="True"
                      ItemsSource="{Binding}"
                      CanUserSortColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Symbol}" Header="Sembol" Width="130"/>
                    <DataGridTextColumn Binding="{Binding Price, StringFormat=N6}" Header="Fiyat" Width="*"/>
                    <DataGridTextColumn Binding="{Binding ChangePercent, StringFormat=N2}" Header="% 24s" Width="*" />
                    <DataGridTextColumn Binding="{Binding Open, StringFormat=N6}" Header="Açılış" Width="*" />
                    <DataGridTextColumn Binding="{Binding High, StringFormat=N6}" Header="Yüksek" Width="*" />
                    <DataGridTextColumn Binding="{Binding Low, StringFormat=N6}" Header="Düşük" Width="*" />
                    <DataGridTextColumn Binding="{Binding Volume, StringFormat=N2}" Header="Hacim" Width="*" />
                    <DataGridTextColumn Binding="{Binding LastUpdateLocal}" Header="Son Güncelleme" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
